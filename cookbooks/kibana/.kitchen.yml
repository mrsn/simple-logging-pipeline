---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-14.04

suites:
  - name: default
    run_list:
      - recipe[kibana::default]
    attributes: { kibana: { version: '4.2.0-linux-x64' } }
