input {
  file {
    path => "/var/log/messages"
    type => "syslog"
  }

  file {
    path => "/var/log/apache2/access.log"
    type => "apache"
  }
}

output {
  stdout { codec => json }
  elasticsearch { hosts => ["192.168.33.51:9200"] }
}
